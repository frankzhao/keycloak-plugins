# Web framework
Flask==3.1.0

# OIDC / OAuth2 client — provides Flask integration and full OIDC support
Authlib==1.4.1

# HTTP client used by Authlib to talk to Keycloak's token / userinfo endpoints
requests==2.32.3

# python-dotenv lets you load .env files when running locally
python-dotenv==1.1.0
